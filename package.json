{
  "name": "main",
  "version": "1.0.0",
  "description": "A simple Telegram bot built with Node.js and Telegraf that checks the availability of a given URL.\nThe bot sends an HTTP request to the provided link and returns the response status code.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "klyaksaoff",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.1",
    "telegraf": "^4.16.3"
  }
}
